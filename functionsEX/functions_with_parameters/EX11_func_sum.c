/* 
 * Procédure qui reçoit deux nombres et
 * affiche leur somme. 
 */
#include <stdio.h>    /* for printf() and fgets() */

int 
main(void) 
{
    int sum(int, int);
    sum(1, 2);
    return(0);
}

int
sum(int n1, int n2)
{
    return(n1 + n2);
}